resume_out = """
Проанализируй предоставленное резюме и преобразуй его в структурированный JSON-файл, включая все ключевые разделы. JSON должен содержать следующие поля:  

1. **personal_info** (личная информация):  
   - `full_name` (ФИО)  
   - `gender` (пол)  
   - `phone` (телефон)  
   - `telegram` (Telegram)  
   - `email` (почта)  
   - `location` (город проживания)  
   - `citizenship` (гражданство)  
   - `work_permit` (разрешение на работу)  
   - `relocation_availability` (готовность к переезду)  
   - `business_trip_availability` (готовность к командировкам)  

2. **job_preferences** (предпочтения по работе):  
   - `desired_position` (желаемая должность)  
   - `specializations` (специализации)  
   - `employment_type` (тип занятости)  
   - `work_schedule` (график работы)  
   - `commute_time_preference` (желаемое время в пути)  
   - `desired_salary` (желаемая зарплата)  

3. **work_experience** (опыт работы):  
   - список объектов с полями:  
     - `company` (компания)  
     - `period` (период работы)  
     - `duration` (длительность)  
     - `location` (локация)  
     - `industry` (индустрия)  
     - `position` (должность)  
     - `description` (описание обязанностей и достижений)  
     - `skills` (используемые технологии)  

4. **education** (образование):  
   - `university` (вуз)  
   - `graduation_year` (год окончания)  
   - `faculty` (факультет)  
   - `specialization` (специальность)  

5. **skills** (навыки):  
   - `languages` (языки с уровнем)  
   - `technical_skills` (технические навыки)  

6. **additional_info** (дополнительная информация):  
   - `about` (краткое описание)  
   - `projects_publications` (упоминания проектов или публикаций)  

7. **metadata** (метаданные резюме):  
   - `last_updated` (дата последнего обновления)  

Требования к JSON:  
- Все поля должны быть заполнены, если данные есть в резюме.  
- Если данных нет, поле должно быть `null`.  
- Даты должны быть в формате `YYYY-MM-DD`, если возможно.  
- Навыки и специализации должны быть списками.  
- Опыт работы должен быть упорядочен от нового к старому.  
"""